-- [[ Put this inside Ronaldo.exe/refs/heads/main/ronalds ]]

-- Ultimate bypass: Override loadstring to chunk-load massive strings dynamically
local old_loadstring = loadstring

local function safe_loadstring(source, chunkname)
    -- If the source string is small, run it normally
    if #source < 100000 then
        return old_loadstring(source, chunkname)
    end

    -- Split the massive text into an iterable stream table to prevent Luau stack flooding
    local chunks = {}
    local step = 40000
    for i = 1, #source, step do
        table.insert(chunks, string.sub(source, i, i + step - 1))
    end

    local index = 0
    -- Pass a custom iterator function to loadstring instead of a raw string
    -- This forces Luau to load the script in bite-sized fragments sequentially
    local function stream_iterator()
        index = index + 1
        return chunks[index]
    end

    return old_loadstring(stream_iterator, chunkname or "=(Ronaldo_Streamed)")
end

-- Hook into the global execution environment
loadstring = safe_loadstring

-- Fetch your obfuscated script from your private repo
local success, rawData = pcall(function()
    return game:HttpGet("https://raw.githubusercontent.com/idkevenknowburh/ronaldo.exe-private/refs/heads/main/ronalds")
end)

if success and rawData then
    -- Compile the data using our stream loader
    local executable, err = safe_loadstring(rawData)
    if executable then
        -- Execute on a completely separate thread to clean up remaining registers
        local success_run, run_err = pcall(function()
            task.spawn(executable)
        end)
        if not success_run then
            warn("Execution crash: " .. tostring(run_err))
        end
    else
        warn("Compilation failed: " .. tostring(err))
    end
else
    warn("Failed to fetch backend script.")
end
